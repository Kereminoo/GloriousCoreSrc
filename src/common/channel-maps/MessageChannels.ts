import { AppChannel } from "./AppChannel.map";
import { EnvironmentChannel } from "./EnvironmentChannel.map";
import { DataChannel } from "./DataChannel.map";
import { DeviceChannel } from "./DeviceChannel.map";
import { ProtocolChannel } from "./ProtocolChannel.map";

export const MessageChannels = { AppChannel, EnvironmentChannel, DataChannel, DeviceChannel, ProtocolChannel };